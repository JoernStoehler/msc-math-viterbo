# Environment configuration for experimental training runs
# Copy this file to .env and fill in the placeholder values.
# Load with `source .env` before running Make targets, or rely on direnv.

# Required by Weights & Biases logging (https://wandb.ai)
WANDB_API_KEY="your-wandb-api-key"
