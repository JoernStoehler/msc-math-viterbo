"""viterbo: PyTorch-first math, datasets, and models.

This package provides a minimal skeleton during the migration to a PyTorch + C++ stack.
"""

# Avoid re-export indirection; import concrete modules from call sites.
