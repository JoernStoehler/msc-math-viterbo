"""Lightweight dataset utilities built on top of the math layer.

Prefer importing submodules explicitly, e.g. ``from viterbo.datasets2 import generators``
or ``import viterbo.datasets2.generators``. Re-export lists and dynamic proxies are
intentionally avoided to keep imports explicit and predictable.
"""

from __future__ import annotations
