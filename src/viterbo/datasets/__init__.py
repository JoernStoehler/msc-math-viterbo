"""Dataset schemas, converters, generators, and wrappers.

Owns HF `Features`, conversions to/from JAX types, and convenience wrappers
that compute derived columns using `viterbo.math` algorithms.
"""

from __future__ import annotations

__all__: tuple[str, ...] = ()
