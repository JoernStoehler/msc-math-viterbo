name: CI

on:
  push:
    branches: [ main, master ]
  pull_request:
    branches: [ main, master ]

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  julia:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Julia
        uses: julia-actions/setup-julia@v2
        with:
          version: '1.11'

      - name: Cache artifacts
        uses: julia-actions/cache@v2

      - name: Instantiate
        run: julia --project=. -e 'using Pkg; Pkg.instantiate()'

      # Formatter rationale: Julia has no general "dev group" in a single Project.toml.
      # We pin JuliaFormatter here (and in Makefile) to keep KISS: one project env,
      # reproducible formatter, no separate .format environment.
      - name: Format check (JuliaFormatter)
        run: |
          julia -e 'using Pkg; Pkg.add(PackageSpec(name="JuliaFormatter", version="1.0.59")); using JuliaFormatter; format(".")'
          git diff --exit-code

      - name: Run tests (with coverage)
        run: julia --project=. -e 'using Pkg; Pkg.test(coverage=true)'

      - name: Process coverage
        uses: julia-actions/julia-processcoverage@v1
        with:
          output: lcov.info

      # Coverage policy: upload-only; no hard threshold enforcement. We'll monitor trends
      # and adjust if needed. Keep CI green during transient Codecov outages.
      - name: Upload coverage to Codecov
        uses: codecov/codecov-action@v4
        with:
          files: lcov.info
          flags: unittests
          verbose: true
          fail_ci_if_error: false
