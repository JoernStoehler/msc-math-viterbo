"""Shared standard polytopes for tests.

The fixtures here intentionally centralize a small, diverse roster of
polytopes so that test modules can import them instead of rebuilding geometry
on the fly.  Each entry stores its geometry, basic invariants, and literature
references when known.
"""
