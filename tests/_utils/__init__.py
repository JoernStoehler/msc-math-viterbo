"""Lightweight test utilities (kept minimal and importable).

This namespace contains small helpers shared across suites. Keep dependencies
minimal and avoid heavy logic.
"""

from __future__ import annotations
